<?xml version="1.0" encoding="UTF-8"?>
<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- Copyright 2026 The Myrmec Authors -->
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
        Edit-resend / regenerate a turn (#104b).

        Adds a soft-supersede flag to ``conversation_messages``. When a user
        edits-and-resends a prior turn, or regenerates an assistant answer,
        the replaced rows are flagged ``superseded = true`` rather than
        deleted: the superseded branch is retained for transparency but is
        excluded from the active branch the engine assembles into the next
        turn's context. Defaults to false; existing rows are back-filled to
        false so the active branch is unchanged.
    -->
    <changeSet id="20260621-01-01" author="myrmec">
        <preConditions onFail="MARK_RAN">
            <not>
                <columnExists tableName="conversation_messages" columnName="superseded"/>
            </not>
        </preConditions>
        <addColumn tableName="conversation_messages">
            <column name="superseded" type="boolean" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
        </addColumn>
    </changeSet>

</databaseChangeLog>
